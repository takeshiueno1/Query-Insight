# AGENTS.md

## 1. この文書の目的

本書は、本リポジトリで作業する開発者およびAIエージェント向けの実務ルールである。
要件、設計、実装、テスト、セキュリティ、保守運用が整合した、ローカルで再現可能な成果物を作ることを目的とする。

本書の記載は、リポジトリ内の実ファイルを優先する。README、設計資料、CI設定、ビルドファイルなどと矛盾する場合は、推測で判断せず、差異を報告して確認すること。

## 2. 現在確認できている状態

- Gitリポジトリは初期化済み。
- 現在の作業ブランチは `agent/initial-query-insight`。
- 初期成果物のコミット履歴が存在する。
- Gitリモート `origin` は `takeshiueno1/Query-Insight` に設定済み。
- `docs/source/original/` にPDF 4件、Excel 4件の原本を格納し、解析済み。
- Reactフロントエンド、Spring Bootバックエンド、PostgreSQL/Flywayスキーマ、テストが存在する。
- 認証、ダッシュボード、社員検索・登録・詳細、自己評価、通知、監査ログはAPI接続済み。
- スキル・経歴・上長評価・承認・管理機能は画面骨格またはDB基盤まで。
- Docker Compose、`.gitignore`、`.env.example`、GitHub Actions CIを追加済み。
- Render Web ServiceとNeon PostgreSQLを対象にした無料公開版の構成を実装済み。外部リソース作成と実デプロイは未実施。

## 3. プロジェクト概要

- プロジェクト名: Query Insight（`README.md` およびディレクトリ名から確認）
- 目的: 社員のスキル・経歴・評価を一元管理し、人材育成と配置・評価判断を支援する。
- 対象ユーザー: 社員、上長・評価者、人事・管理者、システム運用者。
- 提供形態: 社内向けWebアプリケーションとREST API。
- 対象環境: Docker Composeによるローカル環境を実装済み。無料公開版はRender・Neon向け設定まで実装済み。正式環境は要確認。
- 外部サービス: OpenAI APIは任意で、既定無効。SSO、メール、人事基幹連携は未決。

## 4. 技術構成とバージョン

主要構成はJava 21、Spring Boot 4.1.0、Maven Wrapper、React 19.2.x、TypeScript 6.x、Vite 8.x、Node.js 24 LTS、PostgreSQL 18.4、Docker Compose。正確なパッチバージョンは `pom.xml`、`package-lock.json`、`compose.yml` を正とする。

技術を導入する際は、少なくとも以下をREADMEおよびバージョン固定ファイルへ明記すること。

- 言語とランタイムのバージョン
- フレームワークと主要ライブラリのバージョン
- ビルドツールとパッケージマネージャーのバージョン
- データベース、キャッシュ、メッセージング基盤の製品名とバージョン
- Node.jsを使用する場合は `.nvmrc`、`.node-version` などでバージョンを固定する。
- Javaを使用する場合はビルドファイルのtoolchainまたは同等の仕組みでJDKを固定する。
- Pythonを使用する場合は `pyproject.toml` などで対応バージョンと依存関係を管理する。
- Dockerを使用する場合はベースイメージを可変タグだけに依存せず、意図したバージョンへ固定する。

依存ライブラリは既存機能で代替できない場合だけ追加し、ライセンス、保守状況、既知の脆弱性、成果物サイズへの影響を確認すること。

## 5. ディレクトリ構成

主要構成は以下のとおり。

```text
Query Insight/
├── .git/
├── .github/workflows/       # GitHub Actions CI
├── backend/                 # Spring Boot API、Flyway、テスト
├── frontend/                # React/Vite UI、テスト、nginx設定
├── AGENTS.md
├── README.md
├── compose.yml
├── compose.free.yml
├── Dockerfile.free
├── render.yaml
├── .env.example
├── docs/
│   ├── README.md
│   ├── 01-product-overview.md
│   ├── 02-requirements.md
│   ├── 03-use-cases.md
│   ├── 04-non-functional-requirements.md
│   ├── 05-analysis-report.md
│   ├── 06-traceability.md
│   ├── 99-open-questions.md
│   └── source/
│       ├── project-context.md
│       └── original/        # 受領した一次資料8件
└── .gitignore
```

詳細は `README.md` と `docs/README.md` を参照する。生成物、依存パッケージ、ローカル環境ファイル、秘密情報は `.gitignore` の対象とする。

## 6. ビルド、起動、テストコマンド

```powershell
# ローカル一式
Copy-Item .env.example .env
docker compose up --build

# Backendテスト・ビルド
cd backend
.\mvnw.cmd test
.\mvnw.cmd package

# Frontend依存導入・品質検査・ビルド
cd frontend
npm ci
npm run lint
npm test
npm run build

# 無料公開版と同じ統合イメージをローカル確認
cd ..
docker compose -f compose.yml -f compose.free.yml up --build db free-app
```

Flywayはバックエンド起動時に自動適用される。通常のローカルURLは `http://localhost:8088`、統合イメージは `http://localhost:8090`。CIは `.github/workflows/ci.yml` を使用する。正式デプロイ、フォーマット専用コマンド、依存脆弱性ゲートは未設定。

## 7. 作業開始時の確認

変更前に以下を確認すること。

1. `AGENTS.md`、`README.md`、`docs/` の整理済み文書と `docs/source/original/` の原本資料
2. ディレクトリ構成と変更対象の責務
3. ビルドファイル、ロックファイル、使用ランタイム
4. 既存コードの設計、命名、例外処理、テスト方針
5. 現在のブランチ、`git status`、未コミット変更
6. ローカル起動方法、必要な環境変数、外部依存
7. CI/CD、静的解析、フォーマット設定

ユーザーの未コミット変更は削除、上書き、退避しない。依頼と無関係な修正や大規模リファクタリングは行わない。

## 8. 設計方針

技術固有のアーキテクチャは **要確認**。実装時は以下を基本とする。

- 要件と制約を明確にし、最小限の変更で完結させる。
- UI、アプリケーションロジック、ドメインロジック、外部I/Oの責務を分離する。
- 依存方向を一貫させ、循環依存と密結合を避ける。
- 外部API、DB、ファイル、時刻、乱数などの境界を明示し、テスト可能にする。
- 入出力の契約、エラー形式、状態遷移、トランザクション境界を明確にする。
- 後方互換性が必要なAPI・データ形式は、破壊的変更を避ける。必要な場合は移行手順を用意する。
- DB変更では、前方・後方互換性、ロック時間、既存データ移行、ロールバック方法を検討する。
- 並行処理、再試行、二重送信があり得る処理は、冪等性と競合制御を設計する。
- ログ、メトリクス、トレースは障害調査に必要な情報を残しつつ、個人情報や秘密情報を出力しない。

重要な設計判断は、理由、代替案、影響を関連する `docs/` の文書へ記録すること。記録先が確定しない場合は `docs/99-open-questions.md` に確認事項として追加すること。

## 9. コーディング規約

言語固有の規約と自動フォーマッターは **要確認**。構成確定後は、プロジェクト標準のLint・Formatter・型検査を導入し、CIとローカルで同じ設定を使用すること。

共通ルールは以下のとおり。

- 既存の命名、配置、例外処理、テストパターンを優先する。
- 名前は責務と意図が分かるものにし、意味の不明な略語を避ける。
- 関数・クラス・モジュールは単一の責務を持たせ、過度に巨大化させない。
- 公開APIと境界では入力値を検証し、失敗理由を適切なエラーとして返す。
- `null`、空文字、空配列、上限・下限、重複、タイムアウトなどの境界値を扱う。
- 例外を握りつぶさず、利用者向けのエラーと調査用情報を分離する。
- コメントは処理の言い換えではなく、設計理由、制約、注意点を説明する場合だけ記載する。
- TODO、FIXME、空メソッド、ダミーデータ、未使用コード、デバッグ出力を完成成果物に残さない。
- 設定値をコードへ散在させず、環境差分を外部設定として管理する。
- 新規・変更ロジックには、正常系だけでなく境界値と異常系のテストを追加する。

## 10. セキュリティ上の注意

- APIキー、パスワード、秘密鍵、アクセストークン、接続文字列をコードやGit履歴へ含めない。
- `.env` などの実値ファイルは追跡対象外とし、必要なキーだけをダミー値付きの `.env.example` などで示す。
- すべての外部入力を信頼せず、型、長さ、形式、許容値をサーバー側で検証する。
- 認証だけでなく、リソース単位の認可を確認する。IDの付け替えによる不正アクセスを防ぐ。
- SQLはプレースホルダーまたはORMの安全なAPIを使い、文字列連結で組み立てない。
- HTML出力は文脈に応じてエスケープし、危険なHTMLの直接挿入を避ける。
- Cookie、CORS、CSRF、セッション、トークンの設定は利用形態に応じて安全な既定値にする。
- 外部URL取得やファイル処理では、SSRF、パストラバーサル、危険な拡張子、サイズ超過を考慮する。
- エラーレスポンスやログに、スタックトレース、個人情報、認証情報、機密データを露出させない。
- 依存関係の脆弱性検査を行い、重大な指摘は導入・更新前に評価する。
- レート制限、タイムアウト、再試行上限、リクエストサイズ上限を必要に応じて設ける。
- 権限は最小権限とし、本番設定や認証・認可の変更は事前確認を得る。

## 11. Git運用

正式なブランチ戦略、ブランチ保護、レビュー規則は **要確認**。

方針確定までは以下を守ること。

- 作業前後に `git status` と差分を確認する。
- 既定ブランチへ直接コミットするかは要確認。通常は目的ごとの短命な作業ブランチを使用する。
- ブランチ名は内容が分かるものにする。AI作業では、指定がなければ `codex/` 接頭辞を使用する。
- コミットは一つの目的にまとめ、目的が分かる具体的なメッセージを付ける。
- コミット前に、差分、テスト結果、秘密情報や生成物の混入がないことを確認する。
- force push、hard reset、rebaseなど履歴を変更する操作は、明示的な許可なしに行わない。
- タグ作成、リリース、既定ブランチへのマージ、本番デプロイは、運用方針と権限を確認してから行う。
- GitHub反映を依頼された場合は、変更確認、テスト、コミット、push、必要に応じてPull Request作成までを対象とする。

## 12. 禁止事項

- 未確認の要件、バージョン、コマンド、テスト結果を事実として記載すること。
- ユーザーの未コミット変更を無断で削除、上書き、退避すること。
- 依頼と無関係なリファクタリング、依存更新、設定変更を混在させること。
- 秘密情報、個人情報、認証情報をコード、テストデータ、ログ、コミットへ含めること。
- 入力検証や認可をクライアント側だけに依存すること。
- テストを通す目的で検証を無効化したり、例外を握りつぶしたりすること。
- TODO、仮実装、空実装、ダミーデータ、デバッグコードを完成扱いにすること。
- 理由なく新しい依存関係や独自フレームワークを導入すること。
- 本番変更、破壊的DB変更、課金リソース作成、権限変更、履歴改変を事前確認なしに行うこと。
- 実行していないテストやコマンドを実行済みと報告すること。

## 13. テストと検証

変更内容に応じ、利用可能な範囲で以下を実行すること。

- ビルド
- 単体テスト
- 結合テストまたはE2Eテスト
- Lint
- フォーマット検査
- 型チェック
- 依存関係または静的セキュリティ検査
- ローカル起動と主要ユースケースの手動確認

失敗した場合は原因を調査し、依頼範囲内で修正して再実行する。実行できない場合は、理由、未確認の範囲、再現可能な手動確認手順を報告する。

## 14. 作業完了条件

原則として、以下を満たしたときに完了とする。

- 依頼された要件と受け入れ条件を満たしている。
- ビルドが成功している。
- 変更に対応するテストが追加または更新され、成功している。
- Lint、フォーマット、型チェックなど既存の品質ゲートを通過している。
- 正常系、境界値、主要な異常系が確認されている。
- 既存機能への明確な回帰がない。
- 認証・認可、入力検証、秘密情報、ログを含む重大なセキュリティ問題がない。
- ローカルで再現可能な起動・確認手順がある。
- 仕様、設定、API、運用手順の変更に対応するドキュメントが更新されている。
- 不要な生成物、デバッグコード、一時ファイルが残っていない。
- Git差分が依頼範囲に限定され、秘密情報を含んでいない。

アプリケーション実装を追加する作業では、技術構成、正確なコマンド、ディレクトリ構成、環境変数、CI方針を本書へ反映することも完了条件に含める。

## 15. 作業完了時の報告順序

1. 結論
2. 実施した変更
3. 主な変更ファイル
4. 実行したテストと結果
5. ローカル起動・確認手順
6. 未解決事項、制約、確認が必要な点
7. Gitの状態とGitHub反映状況
