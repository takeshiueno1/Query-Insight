<!-- CODEX_MASTER_RULES_BEGIN version=1.0.1 sha256=e7a4ae5ac82de954e36942c691b612c7694a16e9f630c08d89086e3a384e7173 -->
# Codex マスタールール

## 作業契約

- 回答、作業報告、コード内の説明は原則として日本語にする。
- 結論を先に示し、不要な前置き、重複した指示、定型的な進捗報告を避ける。
- 実在するファイル、現在のGit状態、実行結果を根拠にし、推測した構成や未確認の結果を事実として扱わない。
- 確認済み、妥当な前提、未確認を区別する。
- 不明点が重大な分岐でなければ、狭い前提を明記して作業を続ける。
- 要件の矛盾、セキュリティ問題、非効率な設計を発見した場合は、根拠と改善案を示す。
- 回答、説明、レビュー、監査、診断の依頼では読み取りと報告まで行い、変更依頼がない限り実装しない。
- 変更、構築、修正の依頼では、承認済み範囲の実装と必要な検証まで自律的に進める。

## プロンプトの整理

- 作業開始時に、依頼を次の要素へ短く整理する。
  1. 目的と観測可能な成果
  2. 対象範囲と対象外
  3. 参照情報と制約
  4. 承認が必要な境界
  5. 成功条件と必要な証拠
  6. 最終報告形式
- 同じ指示を複数箇所で繰り返さず、結果を左右しない情報をプロンプトへ詰め込まない。
- ユーザーの明示指示を最優先し、ルールから作業範囲や外部操作の権限を勝手に拡張しない。

## 自律実行と承認境界

### 承認なしで進める操作

- 読み取り調査、通常のファイル作成・編集、設定変更。
- 対象テスト、Lint、フォーマット、型チェック、ビルド、ローカル起動。
- 非破壊的なGit確認、差分確認、明示依頼済み範囲の通常commit・push。
- 明確な対象だけの一時ファイル作成と安全な後片付け。
- 依頼範囲内の非破壊的なコマンド、ローカルツール、外部参照。

通常作業でファイルやコマンドごとの確認を繰り返さない。確認待ちが自動化の価値を失わせる場合は、承認境界に該当しない限り自律的に進める。

### 実行直前に承認を求める操作

- 本番環境の変更またはデプロイ。
- 課金が発生するリソース作成、購入、有料API実行。
- データ削除、破壊的DBマイグレーション、復元困難な上書き。
- force push、hard reset、履歴改変、ブランチ削除、大量削除などの破壊的Git・ファイル操作。
- 認証情報、権限、認可、セキュリティ設定の変更。
- 機密情報または個人情報の外部AI・外部サービスへの送信。
- 外部公開、依頼範囲を大きく超える設計変更。

承認要求には、実行内容、対象、影響、費用、復旧方法を簡潔に示す。承認は提示した操作だけに限定し、包括的な将来承認として扱わない。

## コンテキストとRAG

- 次の優先順位で、タスクに必要な範囲だけ段階的に参照する。
  1. 現在の実ファイルとGit状態
  2. README、設計資料、CI、ビルド定義、既存コード
  3. 公式ドキュメント、標準、一次情報
  4. 過去メモ・RAG
- 作業開始時に適用対象の `AGENTS.md`、README、設計資料、ディレクトリ構成、依存関係、Git差分、起動方法を必要な深さで確認する。
- 過去メモは補助情報とし、変更されやすい内容は実ファイルまたは公式情報で再確認する。
- 特定のページ、仕様、ライブラリ、価格、規則、最新情報が関係する場合は、記憶だけで判断せず公式情報を確認する。
- 認証情報、秘密情報、不要な個人情報をマスター、RAG、ログ、評価データへ保存しない。
- 主要な根拠、実行した検証、未確認事項を最終報告へ残し、マスター改善に再利用できる証跡にする。

## 計画・実装・検証

- 複雑な作業だけ短い依存順計画を作り、単純な作業は計画作成の負荷を増やさない。
- 既存の設計、命名、例外処理、テスト方針、依存関係を優先し、依頼と無関係なリファクタリングを行わない。
- 最小限の変更で要件を完全に満たし、ダミー、空実装、省略コード、未完了の印を残さない。
- 新しい依存関係は既存機能で代替できない場合だけ追加し、公式仕様、互換性、保守状況、ライセンス、脆弱性を確認する。
- 変更では、入力検証、認証・認可、秘密情報、例外、トランザクション、境界値、並行実行、冪等性、ログ、性能、互換性、DB移行と復旧への影響をリスクに応じて確認する。
- 機能・不具合修正は、可能な場合に失敗するテストまたは再現手順を先に確認し、最小実装、対象検証、必要な回帰検証の順で進める。
- 小変更は対象テスト、中変更は関連テストとビルド、高リスク変更は全体回帰と実動作確認を行う。
- 失敗を隠さず、製品不具合、テスト不具合、環境問題、既存問題を区別する。
- 実行していないテストや確認を、成功済みと報告しない。

## エージェント数と推論強度

- 原則は1エージェントとする。
- 文言、CSS、設定、文書、影響が明確な1～3ファイル、小さな不具合は1エージェントで差分確認と対象テストまで完了する。
- 複数レイヤー、API契約、状態遷移、中程度の回帰リスクでは1エージェントで実装し、必要な場合だけ独立レビューを1回追加する。
- 認証・認可、個人情報、監査、DB移行、複数の独立モジュールなど複雑・高リスクで、品質または所要時間が明確に改善する場合だけ複数エージェントを使う。
- 同じファイル、スキーマ、DB、ポート、配布物、調査、テストを複数エージェントで重複させない。書込み対象ごとに担当を1人に固定する。
- 最終統合、差分確認、統合後テストは主担当が直接行う。
- 推論強度は簡単な作業=標準、中程度=高、設計・複雑・高リスク=極高を基本とする。
- 実行中セッションのモデル設定を自動変更できない場合は、分析の深さを調整し、次回起動設定を提示する。実際に変更していない設定を変更済みと報告しない。

## Gitと外部反映

- ユーザーの未コミット変更を削除、上書き、退避しない。関連ファイルに重なる場合は差分を読み、両立できないときだけ確認する。
- force push、hard reset、履歴改変、大量削除、ブランチ削除を明示承認なしで実行しない。
- commit・pushが依頼または承認済みの場合は、秘密情報と対象差分を検査し、対象ファイルだけをstageする。
- 既存の未pushコミットを意図せず巻き込む場合はpushせず、専用ブランチまたはworktreeで分離する。
- 本番、外部公開、課金、権限変更はGitHubへの通常push承認とは別の承認境界として扱う。

## マスターと固有ルール

- GitHubの非公開 `codex-master-rules` を唯一の正本とする。
- 生成された `AGENTS.md` のマスター領域を直接編集しない。マスター変更依頼は正本で行い、検証後にcommit・pushする。
- プロジェクト固有ルールは `.codex/PROJECT_RULES.md` に期限なしで保持する。一時例外だけ期限を設定する。
- Codexが新しい恒久ルールを発見した場合は短く提案し、ユーザー承認後に追加する。
- 複数プロジェクトで有効だった固有ルールや失敗対策はマスター改善候補にするが、自動昇格しない。
- 同期失敗時は新版を適用せず、失敗工程、直接原因、現在の安定版、推奨対応だけを簡潔に示す。

## 完了判定と最終報告

- 依頼要件、必要な実装、関連検証、必要文書、差分確認が終わるまで完了扱いにしない。
- 必須検証を実行できない場合は、理由、未確認範囲、再現可能な確認手順を示す。
- 最終報告は必要な項目だけを次の順で簡潔に示す。
  1. 結論
  2. 主な変更とファイル
  3. 実行した検証と結果
  4. 未確認事項・制約
  5. Git・GitHub反映状況

## PETの全作業完了通知

- 依頼された全作業が完了し、未解決、確認待ち、継続作業、未実施の必須検証が残っていない場合だけ、最終回答の末尾に `<!-- BOO_ALL_WORK_COMPLETE -->` を1回付ける。
- 途中報告、部分完了、質問、承認待ち、ブロック中、必須検証未完了の回答には付けない。
- マーカーをコードフェンス、引用、説明例へ記載しない。通知対象となる最終回答の最後にだけ付ける。ただし、システムが最終末尾を必須メタデータブロックに指定している場合は、そのブロックの直前に置く。
- 通知は1タスクにつき1回とし、クリックで閉じる。PET非対応環境だけ通常通知を使用する。
<!-- CODEX_MASTER_RULES_END -->

<!-- CODEX_PROJECT_RULES_BEGIN sha256=b172fdb9ba02629f109b681a87ea0c43f6db6ccfe606e5b1fb484fd0a5fa10ee -->
# Query Insight固有ルール

## プロジェクト概要と技術構成

- 社員のスキル、専門知識、業務経歴、資格、自己評価、上長評価、経営者承認を根拠と履歴付きで管理する社内Webアプリケーションである。
- `backend/` はJava 21、Spring Boot 4.1、Spring Security、OAuth2 Resource Server、Spring JDBC、Flywayを使用する。
- `frontend/` はReact 19.2、TypeScript 6、Vite 8、TanStack Query、React Hook Form、Zodを使用する。
- `compose.yml` はPostgreSQL 18.4、Ollama 0.32.1、ClamAV 1.4.3、backend、frontendで構成し、UIは `http://localhost:8088` を使用する。
- Render/Neon向け無料公開版の設定は存在するが、外部リソース作成、公開、デプロイ、秘密情報登録は事前承認なしに行わない。

## 優先して参照する実在資料

1. `docs/source/original/` の受領一次資料と `docs/source/project-context.md`
2. `docs/06-traceability.md` の要件・画面・実装対応
3. `docs/99-open-questions.md` の未決事項
4. `README.md` と `docs/README.md`
5. `docs/02-requirements.md`、`docs/04-non-functional-requirements.md`、`docs/05-analysis-report.md`
6. `docs/09-security-measures.md`
7. 変更対象の実コード、テスト、Flyway migration、CI、Compose定義

- 実在しない構成や実装済み機能を文書へ追加しない。画面骨格、DB基盤、API接続済み、実動作確認済みを区別する。
- 機能完了を判断する前に `docs/06-traceability.md` を確認し、要件とコードの差分があれば同期する。
- 未決事項を推測で固定ロジックへ埋め込まず、設定、マスターデータ、feature flagまたは確認事項として扱う。

## 業務・セキュリティ上の不変条件

- 認証は短寿命JWT、Refresh Tokenローテーション、再利用検知、アカウントロックを維持する。認可はRBACだけでなく本人・上長割当・部門・全社scopeをAPIで検証する。
- 評価、タレント申請、添付、承認、差戻し、再申請、再オープンは状態遷移、版、楽観ロック、監査ログ、過去履歴を保持する。
- `EXECUTIVE` の全社操作は `scope_type='ALL'` を必要とし、初回競合を含む版競合は409として扱う。
- 添付はPDF/JPEG/PNGの実体、件数・サイズ、アクセスscope、ClamAV検査を確認し、検査成功前に正式公開しない。
- AIは助言用途で、人事判断を自動決定しない。既定はローカルOllama/Qwen3とし、氏名、メール、社員番号、部署名、公開IDを入力へ含めない。
- 実在社員情報を外部AIへ送信しない。外部AI、外部API、Render/Neonへ機密データを送る変更は事前承認を必要とする。
- `.env`、JWT秘密鍵、認証情報、実在社員情報をGit、ログ、マスター、評価データへ保存しない。
- 公開版ではOllamaを無効にし、実在社員の個人情報・評価情報を登録しない。

## 固有の検証コマンド

```powershell
# Backend
Set-Location backend
.\mvnw.cmd test

# Frontend
Set-Location ..\frontend
npm ci
npm run lint
npm test
npm run build

# Compose定義
Set-Location ..
docker compose config
```

- 実動作確認が必要な場合は `docker info`、`docker compose ps`、backend/frontendログ、HTTPの順に確認する。
- Composeを再構築した場合はPostgreSQLとbackendのhealthを待ち、`http://localhost:8088` でログイン、対象API、主要画面を確認する。HTTPエラーはレスポンスの `traceId` とbackendログを照合する。
- 無料公開版相当のローカル検証は `docker compose -f compose.yml -f compose.free.yml up --build db free-app` と `http://localhost:8090` を使用するが、外部デプロイは別の承認境界とする。
<!-- CODEX_PROJECT_RULES_END -->
